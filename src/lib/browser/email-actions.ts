export async function sendAccountConfirmationEmail(_data?: unknown) {
  return {
    success: true,
    message: 'Local demo: account confirmation email was not sent.',
  };
}

export async function sendOrderConfirmationEmail(_data?: unknown) {
  return {
    success: true,
    message: 'Local demo: order confirmation email was not sent.',
  };
}
